# Test Environment Variables
NODE_ENV=test

# Use main database for testing
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ott_users
DB_USER=root
DB_PASSWORD='your-password-with-special-chars'

# Use real Firebase for testing - provide a test token
TEST_FIREBASE_TOKEN=your-test-firebase-token

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000