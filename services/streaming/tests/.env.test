
NODE_ENV=test
SERVICE_NAME=streaming-service-test

# Inherit database config from root .env
DB_HOST=111.118.251.133
DB_PORT=3306
DB_NAME=ott_streaming_test
DB_USER=idealuser
DB_PASSWORD='BVtNpIG*P0v#LnoX'

# Test JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Firebase configuration inherited from root .env
# FIREBASE_PROJECT_ID, FIREBASE_PRIVATE_KEY, FIREBASE_CLIENT_EMAIL will be loaded from root

# Test AWS Configuration (Mock)
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key
AWS_S3_BUCKET=test-bucket
AWS_REGION=us-east-1
